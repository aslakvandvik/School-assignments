# INFO216 — Python, Jupyter, RDF/OWL/SPARQL

Knowledge graph course. Lab notebooks (Lab1, Lab3, Lab10, Lab11, JSON-LD lab) work with RDF triples, OWL ontologies, and SPARQL queries, using the included `.ttl`/`.xml`/`.json` graph and ontology files (e.g. `InvestigationOntology.xml`, `Russia_investigation_kg.ttl`).

- **lab13/** — a worked example notebook using knowledge graph embeddings
