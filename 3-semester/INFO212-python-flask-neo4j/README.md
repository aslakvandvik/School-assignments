# INFO212 — Python, Flask, Neo4j, BPMN

A car rental management system built as a semester project.

- **app/** — the Flask + Neo4j application (`server.py`, models/controllers/templates)
- **bpmn/** — business process model for the car rental workflow (BPMN diagram + Camunda forms)
- `INFO212_assignment_2.pdf`, `oblig_3_INFO212.pdf` — written assignment reports
